service: shopping-service

provider:
  name: aws
  runtime: nodejs6.10

plugins:
  - serverless-webpack

functions:
  suggest:
    handler: shopping-service.suggestWordrobe
    name: ${self:provider.stage}-lambda
    description: Provides wordrobe suggestions based on a given location

    events:
     - http:
        path: suggest
        method: get